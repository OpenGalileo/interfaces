project('simple-cam', 'cpp')

simple_cam = executable('simple-cam',
    'newlibcamera.cpp',
    dependencies: [
        dependency('libcamera'),
        dependency('libpng'),
        dependency('zlib')
        ]
    )